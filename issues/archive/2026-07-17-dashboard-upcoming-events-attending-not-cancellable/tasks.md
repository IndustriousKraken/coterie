- [x] 1.1 In src/web/portal/dashboard.rs upcoming_events, replace the dead <span>Attending</span> and the ad-hoc RSVP <button> with the shared render_rsvp_button from events.rs, passing the AttendanceStatus already fetched via get_member_attendance_status, so the attending state renders a working "Cancel RSVP" control self-wrapped in div.text-right with hx-target="closest div.text-right" and the RSVP↔cancel toggle works in both directions after reload — matching the Events page.
- [x] 1.2 Make render_rsvp_button reachable from dashboard.rs (pub(crate)/shared module) and add a regression test asserting the dashboard's attending fragment contains a cancel control targeting div.text-right.
