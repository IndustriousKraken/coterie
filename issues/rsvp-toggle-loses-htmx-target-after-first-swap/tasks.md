- [ ] 1.1 Make render_rsvp_button emit a single consistent root element that matches the hx-target selector for every status (e.g. always wrap the button/label in `<div class="text-right">…</div>`), so each outerHTML swap replaces and re-emits the same targeted element and repeated RSVP↔cancel toggles keep working.
- [ ] 1.2 Update the events_list_api card render (events.rs:132–134) so the cell is not double-wrapped: drop the separate `<div class="text-right">{}</div>` and render render_rsvp_button's self-wrapped fragment directly (keeping the empty-string case for past events intact).
