# Coterie Staging Environment
# Copy this to /opt/coterie/.env and fill in the values
# Note: Double underscores (__) separate config levels

# Server Configuration
COTERIE__SERVER__HOST=127.0.0.1
COTERIE__SERVER__PORT=8080
COTERIE__SERVER__BASE_URL=https://coterie.yourdomain.com

# Database Configuration (absolute path for systemd)
COTERIE__DATABASE__URL=sqlite:///opt/coterie/data/coterie.db
COTERIE__DATABASE__MAX_CONNECTIONS=5

# Authentication
# Generate with: openssl rand -hex 32
COTERIE__AUTH__SESSION_SECRET=CHANGE_ME_GENERATE_A_RANDOM_STRING
COTERIE__AUTH__SESSION_DURATION_HOURS=24
COTERIE__AUTH__TOTP_ISSUER=Coterie

# Stripe, Discord, and Unifi integrations are disabled by default.
# Omit them entirely or see .env.example for full configuration options.
